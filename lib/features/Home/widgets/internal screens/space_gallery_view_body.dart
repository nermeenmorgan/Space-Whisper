import 'package:flutter/cupertino.dart';

class SpaceGallery extends StatelessWidget {
  const SpaceGallery({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}