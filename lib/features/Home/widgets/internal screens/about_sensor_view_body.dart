import 'package:flutter/cupertino.dart';

class AboutSensor extends StatelessWidget {
  const AboutSensor({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
