import 'package:flutter/cupertino.dart';

class MissionData extends StatelessWidget {
  const MissionData({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
